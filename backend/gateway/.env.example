PORT=3000
REDIS_URL=redis://localhost:6379
PYTHON_WORKER_URL=http://127.0.0.1:8000/query

SUPABASE_URL=
SUPABASE_KEY=
JWT_SECRET=

# Optional — used for password reset / verification emails and ingestion
# notifications. Without these, emails are logged to the console instead of
# actually sent (see backend/gateway/src/utils/mailer.js).
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
FRONTEND_URL=http://localhost:5173
